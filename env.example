# Copy thành .env.local (không commit file .env.local)

# --- Local dev ---
NEXT_PUBLIC_SITE_URL=http://localhost:5173

# --- Supabase (Dashboard → Settings → API) ---
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=paste-anon-public-key-here

# --- Admin (/admin) — email phải trùng user trong Supabase Auth ---
ADMIN_EMAIL=admin@juzlegal.vn
NEXT_PUBLIC_ADMIN_EMAIL=admin@juzlegal.vn

# Mật khẩu admin — chỉ dùng khi chạy npm run supabase:admin (không commit)
# ADMIN_PASSWORD=your-strong-password

# Mật khẩu database — chỉ dùng script SQL local (Dashboard → Settings → Database)
# SUPABASE_DB_PASSWORD=your-db-password

# --- Email thông báo đặt lịch / liên hệ ---
# Resend chưa verify domain → chỉ gửi được tới email đăng ký Resend
BOOKING_NOTIFY_EMAIL=juz.nature2010@gmail.com
# Sau khi verify juzlegal.com trên Resend → đổi thành juz.legal@gmail.com
# RESEND_API_KEY=re_xxxxxxxx  (resend.com → API Keys)
# EMAIL_FROM=JUZ Legal <notifications@yourdomain.com>

# --- Tùy chọn ---
NEXT_PUBLIC_GA_ID=G-NX6GE58D37

# --- Vercel Production (thêm trên vercel.com, không cần trong .env.local) ---
# NEXT_PUBLIC_SITE_URL=https://www.juzlegal.com
# NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=(cùng anon key như trên)
# ADMIN_EMAIL=admin@juzlegal.vn
# NEXT_PUBLIC_ADMIN_EMAIL=admin@juzlegal.vn
